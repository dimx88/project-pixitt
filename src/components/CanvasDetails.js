

// Styles
import './CanvasDetails.css';

export default function CanvasDetails({ canvas }) {
    return (
        <div className="canvasDetails">
            this is the canvas details component...
        </div>
    );
}